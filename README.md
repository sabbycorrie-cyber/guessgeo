# GuessGeo
A geography guessing game built with React and Google Street View API.

## How to run
1. Clone the repository
2. Open terminal
3. Run `cd guessgeo`
4. Run: `npm install`
5. Open `.env.local`file and add our Google Maps API key
6. Run `npm run dev` 
